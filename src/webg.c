/*
@title : webg.c
@author : DEVREAL 
*/

#include "html.h"
#include "css.h"
#include <stdio.h>
#include <stdlib.h>
#include <string.h>

int main(void){
	
	ghtml(); // call function  html generator 
	/*gcss(); 
	gjs();
*/
	FILE * 	f = fopen(const char * index.html, const char * mode);


	if (f != NULL){ 
		fwrite(s, sizeof(char), strlen(s),f)
		fclose(f);

	}else{perror()=NULL; 

	return 0;
}
